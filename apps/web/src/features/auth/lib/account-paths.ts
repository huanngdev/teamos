/*
 * Canonical account routes. Keeping the path in one place prevents the menu,
 * route definition, and redirects from drifting apart.
 */
const accountProfilePath = "/account/profile";

export { accountProfilePath };
