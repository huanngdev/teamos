# Shared Utilities

This directory contains reusable, environment-agnostic utility functions shared by the frontend and backend.

Group utilities by domain or responsibility. Utilities here must be type-safe, side-effect-free where practical, and must not depend on browser-only APIs, database clients, server secrets, or environment variables.
